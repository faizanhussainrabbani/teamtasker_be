namespace TeamTasker.SharedKernel.Interfaces
{
    /// <summary>
    /// Marker interface for aggregate roots
    /// </summary>
    /// <remarks>
    /// In Domain-Driven Design, an aggregate root is an entity that acts as the entry point
    /// to an aggregate (a cluster of domain objects that can be treated as a single unit).
    /// </remarks>
    public interface IAggregateRoot
    {
    }
}
